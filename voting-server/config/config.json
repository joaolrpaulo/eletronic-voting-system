{
    "endpoint": {
        "host": "0.0.0.0",
        "port": 4430
    },

    "debug": true,

    "https": {
        "crt": "certs/voting-server.crt",
        "key": "certs/voting-server.key"
    },

    "database": {
        "schema": "database/schema.sql",
        "location": "database/voting-server.db"
    },

    "sessions": {
        "secret": "certs/flask-secret.key",
        "ttl": 300
    },

    "rsa": {
        "pubkey": "certs/rsa-pub.pem",
        "privkey": "certs/rsa-priv.pem"
    }
}
