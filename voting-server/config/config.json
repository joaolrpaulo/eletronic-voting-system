{
    "endpoint": {
        "host": "0.0.0.0",
        "port": 443
    },

    "debug": true,

    "https": {
        "crt": "certs/voting-server.crt",
        "key": "certs/voting-server.key"
    },

    "database": {
        "schema": "database/schema.sql",
        "location": "database/voting-server.db"
    },

    "jwt": {
        "secret": "certs/jwt-secret.key"
    },

    "tokens": {
        "ttl": 300
    }
}
